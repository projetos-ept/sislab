services:
  api:
    build: .
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      # Persiste o banco SQLite fora do container
      - ./data:/app/data
    environment:
      DATABASE_URL: sqlite:////app/data/sislab.db
