# Copie para .env e ajuste os valores

# URL do banco de dados (padrão: SQLite local)
DATABASE_URL=sqlite:///sislab.db

# Chave de API para autenticação (deixe vazio para desabilitar)
SISLAB_API_KEY=troque-por-uma-chave-segura

# Porta (usado pelo docker-compose)
PORT=5000
