FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 5000

# 1 worker + 2 threads — adequado para VM com pouca RAM
CMD ["gunicorn", "app:app", "--workers", "1", "--threads", "2", "--preload", "--bind", "0.0.0.0:5000"]
